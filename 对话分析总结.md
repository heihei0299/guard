# 对话分析总结

## 问题

用户询问：在 piguard 插件（https://github.com/heihei0299/guard）生效的前提下，为什么 Pi 还能修改其他文件。

用户提供的分析素材是会话导出文件 `piguard插件是否遵守对话探索.html`，其中包含一次完整的 Pi 会话记录（mihomo-manager 项目，Go 语言）。

## 分析过程

### 第一次尝试（错误方向）

我误以为 guard 是指 `pi-permission-system` 扩展（`~/.pi/agent/extensions/pi-permission-system/`），分析了它的配置：

```json
{ "permission": { "*": "allow", "grep": "deny", "find": "deny", ... } }
```

结论是 `"*": "allow"` 默认放行所有工具，write/replace 不在拒绝列表中所以不受限。——**这是完全错误的，用户指的是另一个 guard。**

### 第二次尝试（正确方向）

用户指出正确的仓库 https://github.com/heihei0299/guard，我拉取并分析了源码。

## 正确分析结果

### Guard 设计

三态状态机，通过 Pi 扩展 API 拦截工具调用：

```
normal → (检测到 /skill:to-spec 等目标技能命令) → skill_active → (agent_settled) → guarded
                                                                                      ↓
                                                                          拦截 write/replace/bash
                                                                          白名单：.scratch/ docs/ CONTEXT.md
```

### 未生效的根本原因

**Guard 检测目标技能的方式是精确匹配 `/skill:xxx` 格式的命令字符串，但本次会话中所有技能都是用 `<skill name="...">` XML 标签调用的。**

关键代码（`guard.ts`）：

```typescript
isTargetSkill(command: string): boolean {
    for (const skill of targetSkills) {
        if (command === `/skill:${skill}`) return true;  // 精确匹配
    }
    return false;
}
```

会话中用户输入（entry 4）：

```
<skill name="grill-with-docs" location="...">
References are relative to ...

Run a `/grilling` session, using the `/domain-modeling` skill.
</skill>

将模板文件追加到订阅文件
```

这不是 `/skill:grill-with-docs`，所以 `isTargetSkill()` 返回 `false`，状态机始终停留在 `normal`，所有工具调用畅通无阻。

`handleInput()`、`rebuildFromHistory()` 也使用同样的 `isTargetSkill()` 精确匹配，同样检测不到 XML 标签格式。

### 次要发现

| 问题 | 说明 |
|------|------|
| `edit` 工具有意放行 | 测试中明确写了 `it("allows edit tool (not blocked)")`，当前 Pi 的 `replace` 工具对应 `edit` surface |
| bash `grep` 被标为只读 | `grep` 在 `READONLY_COMMANDS` 集合中，guard 模式下会放行 |
| 「Operation aborted」非 guard 拦截 | 会话中出现的 `Operation aborted` 是用户在 TUI 中手动中断，不是 guard 的行为 |
| `git status` 被标记为只读 | `isGitReadonly()` 将 `git status` 归为只读，guard 模式下会放行 |

### 修复建议

让 guard 也支持 `<skill name="...">` XML 标签格式：

```typescript
isTargetSkill(command: string): boolean {
    if (targetSkills.some(s => command === `/skill:${s}`)) return true;
    const skillTagMatch = command.match(/<skill\s+name=["']([^"']+)["']/);
    if (skillTagMatch) {
        return targetSkills.includes(skillTagMatch[1]);
    }
    return false;
}
```

但需要确认 Pi 的 `input` 事件 `event.text` 是否包含原始消息中的 `<skill>` 标签文本。
