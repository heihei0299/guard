/**
 * Guard Mode extension entry point.
 *
 * Thin forwarder: the implementation lives in `./plan-mode.ts`.
 */
export { createGuard, type GuardExtensionOptions } from "./plan-mode.ts";
export { default } from "./plan-mode.ts";
