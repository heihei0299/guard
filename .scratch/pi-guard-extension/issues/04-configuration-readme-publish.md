# 04 — Configuration + README + publish

**What to build:** 目标技能列表可配置，完善 README 文档，发布到 npm。

**Blocked by:** 02 — Guard state machine + skill detection, 03 — Tool interception + /guard:allow

**Status:** ready-for-agent

- [ ] 支持通过扩展初始化参数或 settings.json 覆盖默认的 5 个目标技能列表
- [ ] 提供配置示例文档
- [ ] 编写 README.md（安装方式、配置说明、用法示例、架构说明）
- [ ] 确保 package.json 的 keywords 包含 "pi-package"
- [ ] 发布到 npm registry
