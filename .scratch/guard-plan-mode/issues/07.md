# 07 — Cleanup & Verification

**What to build:** 删除所有旧 Guard 模块及其测试文件，更新 README.md 反映新 Guard Mode 架构，运行最终的类型检查和测试验证。

**Blocked by:** 06 — Tests

**Status:** resolved

- [x] 删除旧模块：`src/guard.ts`、`src/config.ts`、`src/rule-engine.ts`、`src/bash-command-classifier.ts`、`src/path-normalizer.ts`、`src/permission-config.ts`、`src/prompt-injector.ts`
- [x] 删除对应测试：`src/guard.test.ts`、`src/config.test.ts`、`src/rule-engine.test.ts`、`src/bash-command-classifier.test.ts`、`src/path-normalizer.test.ts`、`src/permission-config.test.ts`、`src/prompt-injector.test.ts`
- [x] 更新 `README.md`：命令参考（`/guard` 替代 `/guard-start`、`/guard:allow`）、配置说明（`~/.pi/agent/pi-guard.json`）、安装和用法示例
- [x] `npx tsc --noEmit` 零错误
- [x] `npm test` 全部通过

## Answer

Ticket 07 完成清理与验证：旧 Guard 模块全部删除，README 按新 Guard Mode 架构重写，类型检查与测试全绿。

### 删除（以工作树实际为准）

- 源文件 6 个：`src/guard.ts`、`src/config.ts`、`src/rule-engine.ts`、`src/bash-command-classifier.ts`、`src/path-normalizer.ts`、`src/permission-config.ts`
- 测试文件 1 个：`src/rule-engine.test.ts`
- 清单中 `src/prompt-injector.ts` 及 6 个旧测试（guard/config/bash-command-classifier/path-normalizer/permission-config/prompt-injector.test.ts）在 Ticket 03/05 中已删除，工作树中不存在，无需操作
- 删除前确认引用链：`guard.ts`→`config.ts`、`rule-engine.ts`→`path-normalizer.ts`、`rule-engine.test.ts`→`rule-engine.ts`；生产代码（plan-mode.ts 等）零引用。删除后 grep 复查旧文件名与旧符号名（`createStateMachine`/`DEFAULT_CONFIG`/`Ruleset`/`expandHomePath` 等）均无残留

### 文档更新

- 根 `README.md` 与 `pi-guard-extension/README.md` 均重写为 Guard Mode 架构：`/guard` 命令参考（含 `--guard` flag，明确 `/guard-start`/`/guard:allow` 已移除）、`plan_mode_question`/`plan_mode_complete` 工具、工具五分类策略、路径白名单（`.scratch/`、`docs/`、`CONTEXT.md`）、bash 安全策略、`~/.pi/agent/pi-guard.json` 配置（`thinkingLevel`/`defaultPlanTools`/`allowedPlanSubagents`）、安装与用法示例、真实模块架构树
- Code review 修复 4 处文档与代码不符：删除不存在的 `/guard continue` 表述（handler 不解析，会落入 `enterPlanModeWithPrompt`）；`defaultPlanTools` 默认值改为只读工具 + bash（非 `read/bash/edit/write`）；`sed`/`awk` 改为无 `-i` 时放行；`CONTEXT.md` 匹配不限于 root（suffix 匹配）。另补架构树缺失的 `tool-selection.ts`/`extension-runtime.ts`
- `safeSubcommands` 配置键有意不写入文档：与 Ticket 06 记录一致（前瞻 API 不生效，不在本 ticket 顺手做）

### 测试与类型检查

- 完整套件：19/19 文件通过（343 passed，391 − 48 个 rule-engine 测试）
- `npx tsc --noEmit`：0 错误
- 删除前基线 391 passed 已确认，删除后无悬空引用

Commit: 见 `map.md`。
